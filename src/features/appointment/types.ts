export interface IAppointment {
    date: string;
    startTime: string;
    id: number;
    clientLogin: string;
    capacity: number;
  }

export interface Table {
    tableId: number;
    capacity: number;
  }
